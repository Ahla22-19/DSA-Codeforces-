t = int(input())
for i in range(t):
    n = int(input())
    m = list(map(int,input().split()))

    a = list(sorted(m)) + list(sorted(m)) 
    b = list(reversed(a))

    k = True
    for i in range(len(a)):
        f = True
        for j in range(n):
            if a[(i + j) % n] != m[j]:
                f = False
                break
        if f:
            print("YES")
            k = False
            break

    if k:

        for i in range(len(b)):
            f = True
            for j in range(n):
                if b[(i + j) % n] != m[j]:
                    f = False
                    break
            if f:
                print("YES")
                break

        else:
            print("NO")



    
    
